spring:
  jpa:
    hibernate:
      ddl-auto: none
      use-new-id-generator-mappings: false
  datasource:
      driverClassName: com.mysql.cj.jdbc.Driver
      url: jdbc:mysql://localhost:3306/commandstore?serverTimezone=America/New_York
      username: paymenttest
      password: secret

server:
  port: 8096

